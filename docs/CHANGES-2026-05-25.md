# Changelog — 25/05/2026

## Infraestrutura

| Item | Antes | Depois |
|------|-------|--------|
| **Apache** | Porta 80, mod-php, security headers | Porta **87**, proxy_fcgi, RemoteIP, sem CSP/XSS/XFO |
| **FPM** | Pool `www` como `www-data` | Pool **dolibarr** como **erpuser** + pool `www` preservado |
| **Firewall** | Nftables vazio | **nftables**: SSH (172.16.254.248), porta 87 (172.16.0.96), resto drop |
| **HTTPS** | — | conf.php configurado: `force_https=1`, URL `https://erp.anderson00.cloudns.ch` |
| **Apache modules** | mod-php habilitado | mod-php removido, `remoteip` + `proxy_fcgi` habilitados |

## Permissões

| Caminho | Antes | Depois |
|---------|-------|--------|
| `htdocs/` (arquivos) | www-data:www-data 644 | **root:www-data** 644 |
| `htdocs/` (diretórios) | www-data:www-data 755 | **root:www-data** 755 |
| `documents/` | www-data:www-data 775 | **erpuser:erpuser** 750 (www-data via grupo erpuser) |
| `conf/conf.php` | www-data:www-data 640 | **root:erpuser** 640 |

## Security Hardening

| Ref | Item | Ação |
|-----|------|------|
| C2 | Debug files | `debug_db_raw.php` + `debug_multicurrency.php` removidos do htdocs, ThemePack, e steps comentados no `custom.sh` |
| C3 | open_basedir | Restrito a `htdocs`, `documents` e `/tmp` no pool dolibarr |
| C4 | mysql -p na CLI | `autoinstall.sh:143` trocado para `export MYSQL_PWD` (oculta senha do ps aux) |
| H3 | ServerTokens | `Prod` (em vez de `OS`), `ServerSignature Off` |
| H4 | Permissions-Policy | Header adicionado: `camera=(), microphone=(), geolocation=(), payment=()` |
| H7 | disable_functions | Bloqueado: `shell_exec, system, passthru, show_source` |
| H8 | fail2ban | Instalado com jails: `apache-auth`, `apache-dolibarr-login`, `sshd` |
| M2 | session.cookie_secure | `= 1` no pool dolibarr |
| M3 | session.cookie_samesite | `= "Lax"` no pool dolibarr |
| M5 | session.use_strict_mode | `= 1` no pool dolibarr |
| M5 | session.cookie_httponly | `= 1` no pool dolibarr |
| M6 | allow_url_fopen | `= Off` no pool dolibarr |
| M9 | documents perms | 750 (dirs) / 640 (files), www-data adicionado ao grupo erpuser |
| M10 | API `/api/` | Restrita a IPs: `127.0.0.1 ::1 172.16.0.96` |

## Scripts Modificados

### autoinstall.sh
- **C4**: `mysql -p"${DB_ADMIN_PASS}"` → `export MYSQL_PWD="${DB_ADMIN_PASS}"` + `unset MYSQL_PWD`

### custom.sh
- **C2**: Steps 16/17 (debug files) comentados permanentemente
- Steps de chown e summary para debug files comentados

## Arquivos Modificados/Removidos

| Arquivo | Ação |
|---------|------|
| `/etc/apache2/sites-available/dolibarr.conf` | Reesrito (porta 87, RemoteIP, proxy_fcgi, Permissions-Policy, sem security headers) |
| `/etc/apache2/ports.conf` | `Listen 80` → `Listen 87` |
| `/etc/apache2/conf-enabled/security.conf` | `ServerTokens Prod`, `ServerSignature Off` |
| `/etc/php/8.4/fpm/pool.d/dolibarr.conf` | **Criado** — pool FPM como erpuser |
| `var/www/dolibarr-23.0.2/htdocs/conf/conf.php` | `prod=1`, `force_https=1`, URL `https://erp.anderson00.cloudns.ch` |
| `/etc/nftables.conf` | Reesrito com regras de firewall restritivas |
| `/etc/fail2ban/filter.d/apache-dolibarr-login.conf` | **Criado** — filtro para login Dolibarr |
| `/etc/fail2ban/jail.local` | **Criado** — jails apache-auth + apache-dolibarr-login |
| `debug_db_raw.php` (htdocs + ThemePack) | **Removido** |
| `debug_multicurrency.php` (htdocs + ThemePack) | **Removido** |
| `libapache2-mod-php8.4` | **Removido** (não mais necessário) |
